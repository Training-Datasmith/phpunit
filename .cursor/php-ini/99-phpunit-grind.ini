memory_limit=-1
zend.assertions=1
error_reporting=-1
log_errors_max_len=0
display_errors=On
; Coverage stack for dogfood baseline (avoid develop — several PHPTs skip when develop is enabled).
xdebug.mode=coverage
pcov.enabled=1
